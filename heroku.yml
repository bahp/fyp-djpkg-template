build:
    docker:
        web: Dockerfile
run:
    web: gunicorn --chdir ./ portfolio.wsgi:application --bind 0.0.0.0:$PORT
    #web: gunicorn --pythonpath portfolio.wsgi:application --bind 0.0.0.0:$PORT # Doenst work
    #web: gunicorn portfolio.wsgi:application --bind 0.0.0.0:$PORT # Works.

#    #web: python /code/manage.py runserver 0.0.0.0:$PORT

#setup:
#  addons:
#    - plan: heroku-postgresql
#      as: DATABASE
#  config:
#    S3_BUCKET: my-example-bucket
#build:
#  docker:
#    web: Dockerfile
#    worker: worker/Dockerfile
#  config:
#    RAILS_ENV: development
#    FOO: bar
#release:
#  command:
#    - ./deployment-tasks.sh
#  image: worker
#run:
#  web: bundle exec puma -C config/puma.rb
#  worker: python myworker.py
#  asset-syncer:
#    command:
#      - python asset-syncer.py
#    image: worker

#Private Spaces do not honor the run section in heroku.yml yet,
#so the command must instead be specified via the Dockerfile.