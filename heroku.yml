build:
    docker:
        web: Dockerfile
run:
    web: gunicorn /usr/src/app/portfolio.wsgi
    #web: gunicorn portfolio.wsgi:application -bind 0.0.0.0:$PORT
    #web: python /code/manage.py runserver 0.0.0.0:$PORT